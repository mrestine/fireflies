{
  "name": "fireflies",
  "version": "0.0.1",
  "private": true,
  "homepage": "https://mrestine.github.io/fireflies",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.98.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
